import { Routes } from '@angular/router';
import { GolfersQuoteComponent } from './golfers-quote.component';

export const routes: Routes = [
    {
        path: '',
        component: GolfersQuoteComponent
    }
];